version: '3'

services:
  db:
    image: proffapt/scmb-db
    container_name: scmb-db
    env_file:
      - .env 
    ports:
      - 5432:5432
    volumes:
      - data:/var/lib/postgresql/data

volumes:
  data:
